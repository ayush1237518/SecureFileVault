# Copy to .env in the project root (d:\SecureFileVault\.env)
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# Optional — LOCAL DEV ONLY. Pins OAuth to localhost when you open the app via 127.0.0.1 or LAN IP.
# Do NOT set this to localhost in Netlify env vars; production uses the live site URL automatically.
# VITE_APP_URL=http://localhost:5173
